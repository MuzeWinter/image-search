# Claude Code 任务单：v2-107 — 无障碍访问 (a11y) 基础支持

遵守全部规则文件：AGENTS.md > CLAUDE.md > docs/AI-CODING-RULES.md

## 目标
为应用添加基础无障碍访问支持，确保键盘导航和屏幕阅读器可用。

## 必须实现
1. 所有按钮添加 `aria-label` 属性
2. 所有输入框添加 `aria-label` 或关联 `<label>`
3. 搜索结果列表支持键盘导航（上下箭头 + Enter）
4. 焦点可见样式（focus-visible outline）
5. 主菜单支持 Tab 键导航

## 不破坏
- 现有所有功能和 UI
- npm run check 全部 8 项 PASS

## 验收标准
- 键盘可完成搜索全流程
- 焦点样式清晰可见
- npm run check 全部 PASS
- 不执行 git commit/push