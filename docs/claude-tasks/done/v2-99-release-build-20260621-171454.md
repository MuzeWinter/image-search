# Claude Code 任务单：v2-99 — 发布构建与安装包自动化

遵守全部规则文件：AGENTS.md > CLAUDE.md > docs/AI-CODING-RULES.md

## 目标
完善发布构建流程，生成可分发的 Windows 安装包。

## 必须实现
1. `npm run build:release` 脚本：生产模式构建 + Rust release 编译
2. 生成 NSIS/MSI 安装包（通过 tauri-bundler）
3. 自动版本号注入（从 package.json 读取）
4. 构建产物校验（文件存在性、大小合理性检查）
5. 构建报告输出到 `release/` 目录

## 不破坏
- 现有 dev 构建流程
- npm run check 全部 8 项 PASS
- 现有所有测试

## 验收标准
- `npm run build:release` 可执行
- 构建产物可验证
- npm run check 全部 PASS
- 不执行 git commit/push