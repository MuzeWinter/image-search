# Claude Code 任务单：012 — 最终集成验收 + 完整测试清单

## 角色

遵守项目全部规则文件。

## 本次目标

按 `提示词.txt` 原始需求的 30 条验收标准逐条检查，修复遗漏。

## 必须实现

### 1. 对照验收标准逐条检查
从 `docs/archive/prototype/提示词.txt`（移动后）读取第 30 节 30 条验收标准。
逐条检查是否已实现。对未实现的条目，补充代码。

### 2. 必须确认的关键功能
- 以图搜图流程完整（拖入图片 → 特征提取 → FAISS 搜索 → 结果显示）
- Excel 内嵌图片提取（openpyxl 提取 → IMG 编号 → 入库）
- UG/CAD 文件扫描（路径记录 → CAD 编号 → 关联图片）
- PDF 文件索引（首页预览 → DOC 编号）
- 匹配管理（自动关联 + 人工确认/拒绝）
- 备份/恢复数据库

### 3. 运行 npm scripts 完整链路
```bash
npm run build     # 必须通过
cargo build       # 必须通过
python backend/main.py  # 必须可启动
```

### 4. 规范检查
- AGENTS.md 要求：无标题栏 ✅（已有）
- 主题切换：深色/浅色完整 ✅
- 中英文：所有文案双语 ✅
- 保存持久化：设置重启保留 ✅

## 建议修改范围

按需补充遗漏功能，不做大范围重构。

## 禁止事项
- 不许删原有功能
- 不许 git commit/push

## 验收标准
- 30 条验收标准全部通过或标注为 Phase 范围外
- `npm run build` 通过
- `cargo build` 通过
