# Claude Code 任务单：v2-04 — 新搜索页面 (拖图+粘贴+结果+打开文件夹)

遵守全部规则文件。

## 目标
重写 Search.tsx 为 v2 聚焦版：拖图 → CLIP搜索 → 结果列表 → 打开文件夹。

## 必须实现
1. 拖拽图片到 dropzone → 触发搜索
2. Ctrl+V 粘贴图片 → 触发搜索
3. 点击 dropzone → 打开文件选择对话框
4. 搜索时显示骨架屏，不阻塞 UI
5. 结果列表：IMG编号 / 相似度% / 来源路径 / UG编号 / 打开按钮
6. Excel来源 → 显示"打开 Excel"按钮
7. UG来源 → 显示"打开 UG 文件夹"按钮
8. 模型未加载时显示加载进度
9. 中英文 i18n
10. 新建 `src/services/systemService.ts`：openFolder/openFile
11. `npm run build` 零错误

## UI 参考
```
┌───────────────────────────────────────┐
│    ┌─────────────────────────┐       │
│    │  拖入图片 或 Ctrl+V 粘贴  │       │
│    └─────────────────────────┘       │
│                                       │
│  #1 [缩略图] IMG-0042  97%           │
│     Excel: 产品清单.xlsx · R342      │
│     UG: TIGER-CLAMP-V3              │
│     [打开 Excel] [打开 UG 文件夹]     │
└───────────────────────────────────────┘
```

## 不执行 git commit/push
